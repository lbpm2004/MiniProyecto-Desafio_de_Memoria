name: desafio_de_memoria
description: Juego de memoria.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Librerias
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.3.5
  animate_do: ^3.3.4
  confetti: ^0.7.0      # Para la lluvia de papeles al ganar
  audioplayers: ^6.0.0  # Para efectos de sonido (flip, acierto, victoria y pérdida)

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/sounds/